import React from 'react';

export const SectionEyebrow = ({ text }: { text: string }) => (
  <div className="font-dm text-[11px] uppercase tracking-[0.15em] text-gray-400 font-bold mb-6">
    {text}
  </div>
);
